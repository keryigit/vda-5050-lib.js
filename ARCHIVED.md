# Archived

This fork has been vendored into `kira_ros` and is no longer maintained here.

The sources now live at `kira_vda5050/vendor/vda-5050-lib/` in the `kira_ros`
repository and are built as a yarn workspace package of `kira_vda5050`. The
yarn-patch workflow (`kira_vda5050/scripts/vda5050_patch.sh`) has been removed.

Last commit imported into `kira_ros`: `174219d` (branch `release/2.6.0`).
Upstream base: `coatyio/vda-5050-lib.js` @ `50981a2` (v1.6.0).

Make library changes in `kira_ros` instead.
